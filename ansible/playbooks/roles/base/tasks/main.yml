---
# Install base packages
- name: Install initial packages
  ansible.builtin.apt:
    name: "{{ required_packages }}"
    state: present
    update_cache: yes
  vars:
    required_packages:
      - curl
      - gnupg
      - ca-certificates
      - apt-transport-https
      - software-properties-common
